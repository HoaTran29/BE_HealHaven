spring:
  application:
    name: healhaven

  # Database Configuration (Supabase PostgreSQL)
  datasource:
    # Using Supavisor Connection Pooling (Transaction Mode port 6543)
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require
    username: postgres.rtahmkkunlwacxdyrnrx
    password: geXHyn6YfpOadJPe
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # OAuth2 Configuration
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1089431720402-svcom6u19il9djr08s81qj1i66oj01et.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email
              - profile
            redirect-uri: "http://localhost:8080/api/login/oauth2/code/{registrationId}"

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:healhaven-secret-key-for-jwt-token-generation-minimum-256-bits}
  expiration: 86400000 # 24 hours in milliseconds
  refresh-expiration: 604800000 # 7 days in milliseconds

# Server Configuration
server:
  port: 8080
  servlet:
     context-path: /api

# Logging
logging:
  level:
    backend.healhaven: DEBUG
    org.springframework.security: DEBUG
